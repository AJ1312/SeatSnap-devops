apiVersion: v1
kind: Service
metadata:
  name: seatsnap-service
  labels:
    app: seatsnap
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80  # Changed to 80 because Nginx serves on port 80
  selector:
    app: seatsnap